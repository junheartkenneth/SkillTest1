#include <iostream>
#include <conio.h>
using namespace std;
int main()
{

int pay
int hour
double tax
tax = (pay * hour) * 0.14

cout << "Enter Pay Rate per Hour"
cin >> pay
cout << "Enter Total Working Hours per Week"
cin >> hour
cout << "Gross Income:" << pay * hour << endl;
cout << "Net Income:" << (pay * hour) - tax << endl;
cout << "Clothes and Other Accessories:" << ((pay * hour) - tax) * 0.10) << endl;
cout << "School Supplies:" << (((pay * hour) - tax) - (((pay * hour) - tax) * 0.10)) * 0.01 << endl;
cout << "saving bonds:" << (((pay * hour) - tax) (((pay * hour) - tax) - (((pay * hour) - tax) * 0.10)) * 0.01) * 0.25 << endl;
cout << "spenditure of parents:" << ((((pay * hour) - tax) (((pay * hour) - tax) - (((pay * hour) - tax) * 0.10)) * 0.01) * 0.25) * 0.50 << endl;

_getch();
return 0;
}




